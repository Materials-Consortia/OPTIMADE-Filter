#!/usr/bin/env perl

use strict;
use warnings;
use OPTiMaDe::FilterParser;
use Data::Dumper;

@ARGV = ( '-' ) unless @ARGV;

for (@ARGV) {
    my $parser = new OPTiMaDe::FilterParser;
    print Dumper( $parser->Run( $_ ) );
}
